import { Range } from './Range';

import { h, mount } from 'bore';

describe(Range.is, () => {

  it('should render', () => {

    console.warn('Missing tests for Range!');

    return mount(
      <Range value={10} />
    ).wait();

  });

});
