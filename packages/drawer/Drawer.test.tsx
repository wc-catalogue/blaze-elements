import { Drawer } from './Drawer';

import { h, mount } from 'bore';

describe(Drawer.is, () => {

  it('should render', () => {

    console.warn('Missing tests for Drawer!');

    return mount(
      <Drawer />
    ).wait();

  });

});
