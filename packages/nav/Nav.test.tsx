import { Nav } from './index';

import { h, mount } from 'bore';

describe( Nav.is, () => {

  it( 'should render', () => {

    console.warn( 'Missing tests for Nav!' );

    return mount(
      <Nav />
    ).wait();

  } );

} );
