import { Bubble } from './Bubble';

import { h, mount } from 'bore';

describe( Bubble.is, () => {

  it( 'should render', () => {

    console.warn( 'Missing tests for Bubble!' );

    return mount(
      <Bubble />
    ).wait();

  } );

} );
