import { Badge } from './Badge';

import { h, mount } from 'bore';

describe(Badge.is, () => {

  it('should render', () => {

    console.warn('Missing tests for Badge!');

    return mount(
      <Badge />
    ).wait();

  });

});
