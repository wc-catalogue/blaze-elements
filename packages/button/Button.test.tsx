import { Button } from './';

import { h, mount } from 'bore';
import { emit } from 'skatejs';

describe( Button.is, () => {

  it( 'should render', () => {

    console.warn('Missing tests for Button!');

    return mount(
      <Button/>
    ).wait();

  });

});
