import { Alert } from './';

import { h, mount } from 'bore';

describe( Alert.is, () => {

  it( 'should render', () => {

    console.warn( 'Missing tests for Alert!' );

    return mount(
      <Alert />
    ).wait();

  } );

} );
