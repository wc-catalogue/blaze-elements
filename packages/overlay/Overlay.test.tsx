import { Overlay } from './Overlay';

import { h, mount } from 'bore';

describe( Overlay.is, () => {

  it( 'should render', () => {

    console.warn( 'Missing tests for Overlay!' );

    return mount(
      <Overlay />
    ).wait();

  });

});
